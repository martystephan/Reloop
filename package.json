{
  "name": "reloop",
  "private": true,
  "version": "0.1.4",
  "description": "Self-hostable feedback SDK + dashboard monorepo",
  "license": "MIT",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
