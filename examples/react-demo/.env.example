# Copy to .env and paste a key created in the dashboard (Project → API Keys)
VITE_RELOOP_KEY=rl_pub_xxx
# Base URL of the Reloop server. Leave empty to use the Vite proxy ("" => same origin).
VITE_RELOOP_ENDPOINT=
